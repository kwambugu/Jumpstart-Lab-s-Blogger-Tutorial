<h1>All tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag), class: 'tag_title', id: "tag_#{tag.id}" %>
      <%= link_to "delete", tag_path(tag.id), method: :delete, data: {confirm: "Really delete the article??"} %>
    </li>
  <% end %>
</ul>
